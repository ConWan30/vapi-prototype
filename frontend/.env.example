# VAPI Dashboard environment variables
# Copy to .env and fill in values before running the dev server

# BridgeAgent API key — must match OPERATOR_API_KEY on the bridge server
# Set OPERATOR_API_KEY env var when starting the bridge, then copy its value here
VITE_BRIDGE_API_KEY=your_operator_api_key_here
