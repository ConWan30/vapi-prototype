# VAPI Contract Gas Report

Generated by: `REPORT_GAS=true npx hardhat test`
Test suite: 120 tests passing
Solc version: 0.8.20 | Optimizer: enabled (200 runs) | Block limit: 60,000,000 gas
Price assumptions: 1000 Gwei gas price | $0.04 USD/IOTX

---

## Method Gas Costs

| Contract | Method | Min Gas | Avg Gas | Max Gas | Avg IOTX @ 1000 Gwei | Avg USD |
|---|---|---:|---:|---:|---:|---:|
| DeviceRegistry | `registerDevice` | 212,858 | 226,241 | 229,958 | 0.000226 IOTX | $0.000009 |
| DeviceRegistry | `deactivateDevice` | — | 48,208 | — | 0.000048 IOTX | $0.000002 |
| DeviceRegistry | `setReputationUpdater` | 46,507 | 46,530 | 46,531 | 0.000047 IOTX | $0.000002 |
| PoACVerifier | `verifyPoAC` | 93,787 | 108,317 | 145,049 | 0.000108 IOTX | $0.000004 |
| PoACVerifier | `verifyPoACBatch` | 157,658 | 244,979 | 336,148 | 0.000245 IOTX | $0.000010 |
| SkillOracle | `updateSkillRating` | 61,333 | 65,298 | 100,965 | 0.000065 IOTX | $0.000003 |
| ProgressAttestation | `attestProgress` | 202,319 | 211,855 | 216,631 | 0.000212 IOTX | $0.000008 |
| TeamProofAggregator | `createTeam` | 137,537 | 156,500 | 230,889 | 0.000157 IOTX | $0.000006 |
| TeamProofAggregator | `submitTeamProof` | 212,403 | 238,328 | 283,124 | 0.000238 IOTX | $0.000010 |
| BountyMarket | `submitEvidence` | — | ~130,000* | — | ~0.000130 IOTX | ~$0.000005 |
| BountyMarket | `claimReward` | — | ~60,000* | — | ~0.000060 IOTX | ~$0.000002 |

*Estimated — not in gas-measured test path (depends on bounty funding state).

---

## Deployment Costs

| Contract | Deploy Gas | % Block Limit | Deploy Cost @ 1000 Gwei |
|---|---:|---:|---:|
| DeviceRegistry | 1,083,591 | 1.8% | 0.001084 IOTX |
| PoACVerifier | 1,112,542 | 1.9% | 0.001113 IOTX |
| BountyMarket | ~1,400,000* | ~2.3%* | ~0.001400 IOTX |
| SkillOracle | 678,485 | 1.1% | 0.000678 IOTX |
| ProgressAttestation | 680,274 | 1.1% | 0.000680 IOTX |
| TeamProofAggregator | 849,436 | 1.4% | 0.000849 IOTX |
| **Total (all 6)** | **~5,804,000** | **~9.7%** | **~0.005804 IOTX** |

*BountyMarket deployment not measured in gas report run (uses `PoACVerifierTestable` substitute).

---

## Session Cost Estimates (DualShock, 1-hour session)

Assumes 1 record/second, 3600 records/session, batched at 10 records/batch.

| Operation | Calls/session | Gas/call (avg) | Total gas | Total IOTX |
|---|---:|---:|---:|---:|
| `registerDevice` (once per identity) | 1 | 226,241 | 226,241 | 0.000226 |
| `verifyPoACBatch` (10 records/batch) | 360 | 244,979 | 88,192,440 | 88.192 |
| `updateSkillRating` (session end) | 1 | 65,298 | 65,298 | 0.000065 |
| `attestProgress` (if improvement) | 0–1 | 211,855 | 0–211,855 | 0–0.000212 |
| **Session total** | | | **~88.5M gas** | **~88.2 IOTX** |

> **Note:** Batch verification dominates cost. Increasing batch size from 10 to 50 would
> reduce chain round-trips from 360 to 72 and improve throughput. IoTeX testnet faucet
> provides 1000 IOTX — sufficient for ~11 full 1-hour sessions at default settings.

---

## Notes

- Gas costs computed from Hardhat local network with optimizer enabled (200 runs).
- IoTeX mainnet/testnet gas prices vary; 1000 Gwei is a conservative testnet estimate.
- USD estimate uses $0.04/IOTX (approximate at time of writing; verify current price).
- The `viaIR: true` compiler setting on PoACVerifier resolves stack-too-deep; it does
  not materially affect runtime gas (only compilation).
- All six contracts fit comfortably within IoTeX's 60M gas block limit for deployment.

---

## Reproducing This Report

```bash
cd contracts
REPORT_GAS=true npx hardhat test
```

Requires `hardhat-gas-reporter` in `devDependencies` (already present) and
`REPORT_GAS=true` environment variable to activate the reporter.


---

## Gas Cost Estimates -- Phase 37 Additions

The following estimates cover the Phase 37 contracts and methods added in PHGCredential and TournamentGateV3. Figures are based on opcode-level analysis; see "Known Limitations" below.

| Contract | Method | Estimated Gas | Notes |
|---|---|---:|---|
| PHGCredential | `suspend()` | ~45,000 | 3 SSTORE writes (isSuspended, suspendedUntil, suspensionEvidence) + CredentialSuspended event |
| PHGCredential | `reinstate()` | ~30,000 | 3 SSTORE writes (clear suspension state) + CredentialReinstated event |
| PHGCredential | `isActive()` | ~2,500 | 2 SLOAD reads (isSuspended + suspendedUntil) -- view function, no state change |
| TournamentGateV3 | `assertEligible()` | ~80,000 | V2 gate checks + external `isActive()` call to PHGCredential (~2,500 gas) + overhead |
| AlertRouter webhook dispatch | N/A | N/A | Off-chain only -- dispatches via HTTP from bridge process; zero on-chain gas cost |

---

## Known Limitations

- All gas figures are estimates based on opcode analysis. Actual costs depend on the IoTeX gas schedule for the P256 precompile at address `0x0100`. IoTeX's gas schedule for precompiles may differ from Ethereum mainnet; verify against the current IoTeX documentation before using these figures in production cost estimates.
- The P256 precompile `staticcall` must stay under 100,000 gas per individual verification. Calls that exceed this limit will revert or exceed block gas constraints on networks with tighter per-call caps.
- Batch verification (10 records per `verifyPoACBatch` call) should stay under 1,000,000 gas for a single transaction to remain safely within IoTeX's 60M gas block limit and leave room for other transactions in the same block.
- Gas figures in this report have NOT been measured against a running Hardhat node with the Phase 37 contracts deployed. Do not use these figures in production cost estimates without first benchmarking against a live node: `REPORT_GAS=true npx hardhat test`.
