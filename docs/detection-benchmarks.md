# VAPI PITL Detection Benchmarks

> **CRITICAL DISCLAIMER**: All performance figures in this document are derived from
> **synthetic test data generated by `tests/data/realistic_generators.py`**.
> These figures have **no real-world validity** until validated against live
> DualShock Edge hardware data captured with `scripts/capture_session.py`.
> Every detection percentage below must be read as "on synthetic test patterns only".

---

## 1. Threshold Status

All PITL thresholds are currently **magic numbers** calibrated on synthetic data.
The table below documents each threshold, its current value, its origin, and the
calibration work required before hardware validation.

| Layer | Threshold | Current Value | Origin | Status |
|-------|-----------|--------------|--------|--------|
| L4 | Mahalanobis anomaly | 3.0 | Synthetic calibration | **Needs real-hardware calibration** |
| L4 | Continuity threshold | 2.0 | Synthetic calibration | **Needs real-hardware calibration** |
| L4 | Diagonal covariance | N/A | Assumption (features independent) | **Needs full covariance matrix after N≥50 sessions** |
| L5 | CV threshold | 0.08 | Literature + synthetic | **Needs real-hardware calibration** |
| L5 | Entropy threshold | 1.5 bits | Literature + synthetic | **Needs real-hardware calibration** |
| L5 | Quantization threshold | 0.55 | Synthetic calibration | **Needs real-hardware calibration** |
| L3 | Classifier confidence | Default sklearn | sklearn default | **Needs real adversarial training data** |
| Behavioral | Warmup sigmoid scale | 20000 | Empirical on synthetic | **Needs real-hardware session slope distributions** |
| Behavioral | Burst score formula | CV / 2.0 | Heuristic | **Needs real-hardware CV distributions** |
| DBSCAN | Epsilon | 1.0 | Heuristic | **Needs device population data** |
| DBSCAN | min_samples | 3 | Heuristic | **Needs device population data** |

---

## 2. Synthetic Attack Coverage

Each synthetic attack type and which PITL layer catches it (on synthetic data):

| Attack Type | Generator | Primary Detection Layer | Expected Detection Rate* |
|-------------|-----------|------------------------|--------------------------|
| Software macro (constant timing) | `generate_macro_session` | L5 (CV < 0.08) | ~100%* |
| Stick aimbot (snap to target) | `generate_aimbot_session` | L3 (jerk distribution) | ~95%* |
| HID injection (zero IMU) | `generate_injection_session` | L4 (Mahalanobis: IMU features near-zero) | ~100%* |
| Warmup attack (gradual improvement) | `generate_warmup_attack_session` | BehavioralArchaeologist (correlated slopes) | ~80%* |
| Timestamp replay | `generate_replay_attack_session` | PITLSessionRegistry (nullifier anti-replay) | ~100%* |

*"on synthetic test patterns only" — not validated on real hardware.

---

## 3. Synthetic False Positive Analysis

Expected false positive rates per skill tier (all synthetic data):

| Skill Tier | L4 FP Rate* | L5 FP Rate* | Combined FP Rate* |
|------------|-------------|-------------|-------------------|
| Bronze | ~0%* | ~0%* | ~0%* |
| Gold | ~0%* | ~0%* | ~0%* |
| Diamond | Unknown | ~5% risk* | Unknown |

> Diamond-tier players have tight timing variance that approaches L5 threshold.
> Real-hardware validation is essential before deploying diamond-tier gates.

---

## 4. Synthetic Generator Parameters

### Human session model
- **Reaction time**: `lognormal(μ=5.7, σ=0.3×modifier)` — median ~300ms, range 150–800ms
- **Stick movement**: Brownian walk toward random target with jitter proportional to speed
- **Trigger**: ramp + micro-tremor (3-8 Hz, ~2% amplitude) — models physiological tremor
- **IMU**: Brownian motion + movement-correlated acceleration + thermal drift
- **Fatigue**: timing variance increases over session duration

### Macro session model
- **Timing**: constant 50ms interval (CV = 0.0)
- **IMU**: zero (controller not physically moved)
- **Detection signal**: L5 CV < 0.08 + zero IMU

### Aimbot session model
- **Right stick**: instantaneous snap to random target (zero transition frames at aggression=1.0)
- **Left stick**: normal human movement
- **IMU**: normal (controller is physically held)
- **Detection signal**: L3 right-stick jerk distribution

### Injection session model
- **Sticks**: smooth sinusoidal (software-generated, plausible)
- **IMU**: zero (dead giveaway — injected HID doesn't include real sensor data)
- **Detection signal**: L4 Mahalanobis (IMU features near-zero while sticks active)

### Warmup attack model
- **Progress**: humanity_proxy linearly increases from 0.1 → 0.8 across N sessions
- **IMU**: starts near-zero, gains noise as attack progresses (mimics warming up)
- **Detection signal**: BehavioralArchaeologist correlated positive slopes

---

## 5. Path to Real-World Benchmarks

To replace synthetic benchmarks with real-world data:

1. **Connect DualShock Edge via USB**: `scripts/hardware_setup.sh`
2. **Capture baseline sessions** (minimum 10): `python scripts/capture_session.py --duration 60`
3. **Run threshold calibrator**: `python scripts/threshold_calibrator.py sessions/*.json`
4. **Apply calibration profile** to bridge: set `calibration_profile.json` path in config
5. **Re-run benchmark tests** with real data — update this document with empirical figures
6. **Test at each skill tier** (bronze through diamond) with N≥20 sessions per tier
7. **Test each attack type** with N≥10 attack sessions

Minimum acceptable targets for production deployment:
- False positive rate < 5% at all human skill tiers
- Detection rate > 95% for macro/injection attacks (clear signals)
- Detection rate > 80% for aimbot/warmup attacks (ambiguous signals)
- All thresholds must have 95% confidence intervals from N≥50 real sessions

---

## 6. Running the Benchmark Tests

```bash
# Run synthetic benchmark tests (fast, no hardware required)
python -m pytest tests/test_detection_realistic.py -v

# Run with coverage
python -m pytest tests/test_detection_realistic.py --cov=tests/data -v

# Run hardware benchmarks (requires connected DualShock Edge)
pytest tests/hardware/ -v -m hardware
```

---

*Document status: Pre-hardware-validation. All figures synthetic. Updated post-calibration required.*
